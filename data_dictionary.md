# Data Dictionary

## SaaS Product Analytics & Churn Prediction

This document describes the structure, meaning, and intended use of all raw datasets used in the project.

The datasets are **synthetically generated for educational and portfolio purposes**. They simulate a realistic SaaS business environment containing users, product usage events, subscription activity, and customer support interactions.

---

## 1. Dataset Overview

| Dataset | Grain | Approx. Rows | Primary Key | Purpose |
|---|---|---:|---|---|
| `users.csv` | One row per user | 30,000 | `user_id` | Customer and user profile information |
| `events.csv` | One row per product event | 1.55M+ | `event_id` | Product usage and behavioral activity |
| `subscriptions.csv` | One row per user subscription | 30,000 | `subscription_id` | Subscription, revenue, and churn information |
| `support_tickets.csv` | One row per support ticket | 9,976 | `ticket_id` | Customer support experience |

---

# 2. `users.csv`

## Purpose

Contains the core profile information for users of the fictional SaaS platform.

The table represents the **user dimension** and is used as the central reference table for product usage, subscriptions, and support activity.

## Grain

**One row = one user**

## Primary Key

`user_id`

## Columns

| Column | Data Type | Description |
|---|---|---|
| `user_id` | String | Unique identifier for each user |
| `company_id` | String | Identifier of the company the user belongs to |
| `signup_date` | Date | Date when the user registered |
| `country` | String | User's country |
| `industry` | String | Industry of the user's company |
| `company_size` | String | Size category of the company |
| `acquisition_channel` | String | Marketing/acquisition source through which the user joined |
| `plan` | String | User's current SaaS plan |
| `role` | String | User's role within the company |
| `primary_device` | String | User's primary device |
| `engagement_propensity` | Numeric | Synthetic behavioral propensity score between 0 and 1 used to simulate realistic engagement |
| `onboarding_completed` | Boolean | Indicates whether the user completed onboarding |

### Expected Values

#### `plan`

```text
Free
Starter
Professional
Enterprise
role
Admin
Manager
Member
Viewer
primary_device
Desktop
Mobile
Tablet
acquisition_channel
Organic Search
Paid Search
Referral
Social Media
Content Marketing
Partner
Email
Direct
```
## 3. `events.csv`

### Purpose

Contains detailed product-usage events generated by users while interacting with the SaaS platform.

This is the project's **primary behavioral and event-level dataset**.

It is used for:

* Product analytics
* DAU / WAU / MAU
* Engagement analysis
* Session analysis
* Feature adoption
* Funnel analysis
* Retention analysis
* Churn feature engineering

### Grain

> **One row = one user product event**

### Primary Key

`event_id`

### Columns

| Column                     | Data Type | Description                                          |
| -------------------------- | --------- | ---------------------------------------------------- |
| `event_id`                 | String    | Unique identifier for the event                      |
| `user_id`                  | String    | User who generated the event                         |
| `company_id`               | String    | Company associated with the user                     |
| `event_timestamp`          | Datetime  | Exact date and time when the event occurred          |
| `event_type`               | String    | Specific action performed by the user                |
| `feature`                  | String    | Product area associated with the event               |
| `session_id`               | String    | Identifier representing the user's session           |
| `device`                   | String    | Device used during the event                         |
| `session_duration_minutes` | Numeric   | Estimated duration associated with the event/session |

### Event Types

The `event_type` column contains the following values:

* `login`
* `onboarding_started`
* `onboarding_completed`
* `project_created`
* `task_created`
* `task_completed`
* `file_uploaded`
* `comment_added`
* `team_member_invited`
* `report_generated`
* `subscription_started`
* `subscription_upgraded`
* `subscription_downgraded`
* `support_viewed`

### Feature Categories

| Feature            | Related Events                                                             |
| ------------------ | -------------------------------------------------------------------------- |
| Authentication     | `login`                                                                    |
| Onboarding         | `onboarding_started`, `onboarding_completed`                               |
| Project Management | `project_created`                                                          |
| Task Management    | `task_created`, `task_completed`                                           |
| File Management    | `file_uploaded`                                                            |
| Collaboration      | `comment_added`, `team_member_invited`                                     |
| Reporting          | `report_generated`                                                         |
| Billing            | `subscription_started`, `subscription_upgraded`, `subscription_downgraded` |
| Support            | `support_viewed`                                                           |

---

# 4. `subscriptions.csv`

### Purpose

Contains subscription-level information for users.

This dataset supports:

* MRR analysis
* ARR analysis
* ARPU
* Subscription plan analysis
* Upgrade/downgrade analysis
* Customer tenure
* Churn analysis
* Revenue segmentation

### Grain

> **One row = one user subscription record**

### Primary Key

`subscription_id`

### Foreign Keys

* `user_id`
* `company_id`

### Columns

| Column                      | Data Type | Description                                                 |
| --------------------------- | --------- | ----------------------------------------------------------- |
| `subscription_id`           | String    | Unique subscription identifier                              |
| `user_id`                   | String    | User associated with the subscription                       |
| `company_id`                | String    | Company associated with the subscription                    |
| `plan`                      | String    | Subscription plan                                           |
| `subscription_start_date`   | Date      | Date the subscription began                                 |
| `subscription_end_date`     | Date      | Date the subscription ended; blank for active subscriptions |
| `billing_frequency`         | String    | Billing cycle                                               |
| `monthly_recurring_revenue` | Numeric   | Monthly recurring revenue generated by the subscription     |
| `annual_contract_value`     | Numeric   | Annualized contract value                                   |
| `trial`                     | Boolean   | Indicates whether the customer entered through a trial      |
| `status`                    | String    | Current subscription status                                 |
| `acquisition_source`        | String    | Source through which the subscription/customer was acquired |
| `upgrade_count`             | Integer   | Number of recorded subscription upgrades                    |
| `downgrade_count`           | Integer   | Number of recorded subscription downgrades                  |
| `tenure_days`               | Integer   | Number of days the subscription has been active             |

### Plans

* `Free`
* `Starter`
* `Professional`
* `Enterprise`

### Billing Frequency

* `None`
* `Monthly`
* `Annual`

### Status

* `Free`
* `Active`
* `Churned`

---

# 5. `support_tickets.csv`

### Purpose

Contains customer support interactions.

This dataset allows us to investigate whether **customer support experience is associated with engagement and churn**.

It is particularly useful for:

* Support volume analysis
* Resolution-time analysis
* Customer satisfaction
* Ticket severity
* Reopened tickets
* Churn-risk analysis

### Grain

> **One row = one support ticket**

### Primary Key

`ticket_id`

### Foreign Keys

* `user_id`
* `company_id`

### Columns

| Column                          | Data Type | Description                                     |
| ------------------------------- | --------- | ----------------------------------------------- |
| `ticket_id`                     | String    | Unique support ticket identifier                |
| `user_id`                       | String    | User who created the ticket                     |
| `company_id`                    | String    | Company associated with the ticket              |
| `created_at`                    | Datetime  | Date and time when the ticket was created       |
| `category`                      | String    | Type of customer support issue                  |
| `priority`                      | String    | Priority/severity of the ticket                 |
| `channel`                       | String    | Support channel used                            |
| `status`                        | String    | Current ticket status                           |
| `resolved_at`                   | Datetime  | Date and time when the ticket was resolved      |
| `resolution_time_hours`         | Numeric   | Time required to resolve the ticket             |
| `reopened`                      | Boolean   | Indicates whether the ticket was reopened       |
| `satisfaction_score`            | Integer   | Customer satisfaction rating from 1 to 5        |
| `plan_at_ticket`                | String    | Customer's plan when the ticket was created     |
| `subscription_status_at_ticket` | String    | Subscription status when the ticket was created |

### Support Ticket Categories

* `Billing`
* `Technical Issue`
* `Account Access`
* `Feature Request`
* `Performance`
* `Integration`
* `Data/Reporting`
* `How To`

### Priority Levels

* `Low`
* `Medium`
* `High`
* `Urgent`

### Support Channels

* `Email`
* `In-App Chat`
* `Web Form`
* `Phone`

### Ticket Status

* `Resolved`
* `Open`

### Satisfaction Score

| Score | Meaning           |
| ----: | ----------------- |
|     1 | Very dissatisfied |
|     2 | Dissatisfied      |
|     3 | Neutral           |
|     4 | Satisfied         |
|     5 | Very satisfied    |

---

# 6. Key Identifiers

The following identifiers connect the datasets.

| Identifier        | Description                       | Appears In                                    |
| ----------------- | --------------------------------- | --------------------------------------------- |
| `user_id`         | Unique user identifier            | Users, Events, Subscriptions, Support Tickets |
| `company_id`      | Unique company identifier         | All four datasets                             |
| `event_id`        | Unique product-event identifier   | Events                                        |
| `subscription_id` | Unique subscription identifier    | Subscriptions                                 |
| `ticket_id`       | Unique support-ticket identifier  | Support Tickets                               |
| `session_id`      | Unique product-session identifier | Events                                        |

---

# 7. Dataset Relationships

The logical relationships between the datasets are based primarily on `user_id` and `company_id`.

## User-Level Relationships

```text
                         users
                           │
             ┌─────────────┼─────────────┐
             │             │             │
          user_id        user_id       user_id
             │             │             │
             ▼             ▼             ▼
          events      subscriptions   support_tickets
```

### User-Level Relationship Mapping

```text
users.user_id
      │
      ├──────── events.user_id
      │
      ├──────── subscriptions.user_id
      │
      └──────── support_tickets.user_id
```

## Company-Level Relationships

```text
users.company_id
       │
       ├──────── events.company_id
       │
       ├──────── subscriptions.company_id
       │
       └──────── support_tickets.company_id
```

Detailed relationship documentation will be provided in:

`data_model.md`

---

# 8. Data Quality Expectations

Before performing analysis, the following data-quality checks should be performed.

## Users

* `user_id` should be unique.
* `user_id` should not be null.
* `signup_date` should contain valid dates.
* `engagement_propensity` should be between `0` and `1`.
* `onboarding_completed` should contain Boolean values.

## Events

* `event_id` should be unique.
* `user_id` should exist in `users.csv`.
* `event_timestamp` should contain valid datetime values.
* Events should occur on or after the associated user's signup date.
* `session_duration_minutes` should not be negative.

## Subscriptions

* `subscription_id` should be unique.
* `user_id` should exist in `users.csv`.
* Subscription end dates should not precede subscription start dates.
* `monthly_recurring_revenue` should not be negative.
* `tenure_days` should not be negative.

## Support Tickets

* `ticket_id` should be unique.
* `user_id` should exist in `users.csv`.
* `resolved_at` should be after `created_at` when present.
* `resolution_time_hours` should not be negative.
* `satisfaction_score` should be between `1` and `5`.

---

# 9. Synthetic Data Disclaimer

All datasets in this project are **synthetically generated**.

They are designed to simulate realistic SaaS business behavior for:

* Portfolio development
* Data analytics practice
* Product analytics
* Machine learning
* Business intelligence
* Customer retention analysis

The data does **not** represent actual customers, companies, or transactions.

The synthetic generation process intentionally introduces behavioral relationships between:

* Product engagement
* Subscription activity
* Support interactions
* Customer churn

This allows meaningful analytical and machine-learning questions to be investigated.

---

# 10. Intended Analytical Use

The datasets will be used to answer questions across five major analytical areas:

```text
Product Analytics
       ↓
User Engagement
       ↓
Retention & Cohort Analysis
       ↓
Revenue & Subscription Analytics
       ↓
Churn Analysis & Prediction
```

## Overall Analytical Objective

The final project will combine these datasets to understand:

> **How users interact with the SaaS product, what drives engagement and retention, what factors are associated with churn, and which customers may be at risk of leaving.**

### Key Analytical Themes

| Area              | Main Focus                                     |
| ----------------- | ---------------------------------------------- |
| Product Analytics | Feature usage, events, sessions, adoption      |
| User Engagement   | DAU, WAU, MAU, frequency, activity             |
| Retention         | Cohort analysis, retention rates, active users |
| Revenue           | MRR, ARR, ARPU, plan performance               |
| Subscriptions     | Upgrades, downgrades, tenure, churn            |
| Support           | Ticket volume, resolution time, satisfaction   |
| Churn             | Churn drivers, risk factors, prediction        |

---

# File Information

| Property             | Value                                                     |
| -------------------- | --------------------------------------------------------- |
| **Project**          | SaaS Product Analytics & Churn Prediction                 |
| **Data Type**        | Synthetic                                                 |
| **Primary Language** | Python                                                    |
| **Analysis Type**    | Product Analytics + Customer Analytics + Machine Learning |
| **Main Objective**   | Understand engagement, retention, revenue, and churn      |

---

## Related Documentation

* `data_dictionary.md` — Dataset and column definitions
* `data_model.md` — Dataset relationships and data modeling
* `business_questions.md` — Business questions and analytical objectives
* `README.md` — Project overview and setup instructions
